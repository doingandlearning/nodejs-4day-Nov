"use strict";

const add = (a: number, b: number): number => a + b;

function subtract(a: number, b: number): number {
  return a - b;
}

export { add, subtract };

// module.exports = {
//   add,
// };
